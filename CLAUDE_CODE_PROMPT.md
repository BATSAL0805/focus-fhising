# 손맛 — 찌낚시 포커스 타이머 웹앱 제작 요청

## 프로젝트 개요

"손맛"이라는 이름의 집중(포모도로) 타이머 웹앱을 만들어줘. 컨셉은 Focus Flight류의 "집중하는 동안 뭔가 진행되고 끝나면 보상 이벤트가 터지는" 타이머를, 낚시(찌낚시)로 바꾼 것.

핵심 루프: 집중 시간을 설정하고 "찌 던지기"로 시작 → 집중하는 동안 물 위에 찌가 떠 있는 대기 화면 → 타이머 종료 시 입질 이벤트 → 챔질(타이밍 반응) → 릴링(미니게임) → 결과 화면에서 물고기 획득 → 도감에 자동 등록 → 통계에 누적.

## 기술 스택

- 프레임워크 없이 순수 HTML/CSS/JS로 시작 (가볍게, 이후 필요하면 확장)
- 데이터는 전부 localStorage에 저장 (세션 통계, 도감 수집 현황, 연속 기록 등)
- 반응형으로 만들되 기본 타깃은 모바일 폭(약 390px 프레임 기준으로 디자인함)
- 사운드는 Web Audio API 또는 `<audio>` 태그로 나중에 붙일 수 있게 구조만 열어둘 것
- 지금 단계에서는 계정/서버/백엔드 없이 로컬 단독 동작

## UI 참고자료

같이 첨부한 `ui-reference` 폴더의 7개 정적 HTML이 화면 디자인 레퍼런스야. 브라우저로 직접 열어보면 실제 레이아웃/색상/폰트/아이콘을 확인할 수 있어. **이 파일들은 정적 목업이라 아무 기능이 없고, 애니메이션도 CSS로 흉내만 낸 상태**야 — 실제 상태 전환, 타이머 로직, 이벤트 처리는 전혀 없으니 그대로 복붙하지 말고 이 디자인을 최대한 그대로 살려서 실제로 동작하는 코드로 새로 구현해줘.

화면 매핑:
1. `01-start.html` — 시작 화면 (집중 시간 다이얼, 프리셋, 미끼 선택, "찌 던지기" CTA, 하단 탭바)
2. `02-focus.html` — 집중 대기 화면 (물결 배경, 찌가 떠 있는 애니메이션, 큰 카운트다운, 진행 바, 일시정지/중단 버튼)
3. `03-bite.html` — 입질/챔질 이벤트 화면 (찌가 물속으로 들어가는 연출, "챔질!" 탭 버튼)
4. `04-reeling.html` — 릴링(물고기 당기기) 화면 (장력 게이지, 릴 감기 진행률)
5. `05-result.html` — 결과 화면 (잡은 물고기 카드, 등급 배지, 세션 통계, "도감에 저장"/"다시 낚시하기" 버튼)
6. `06-collection.html` — 도감 화면 (수집률, 등급 필터 탭, 어종 그리드, 미포획은 실루엣 처리)
7. `07-stats.html` — 통계 화면 (총 집중시간/총 조과, 연속 기록, 요일별 막대 그래프, 최근 조황 리스트)

디자인 톤: 청록·네이비 계열 수면 색(#0d2438, #1b4d54, #2f7a80 등)과 따뜻한 우드톤 포인트(#a9713f, #e08a3c). 폰트는 Google Fonts의 'Gowun Batang'(제목/숫자용 세리프)과 'Gowun Dodum'(본문용 산세리프) 조합. 이 색상/폰트 팔레트를 코드 전체에 CSS 변수로 정리해서 재사용해줘.

## 핵심 기능 명세

### 1단계 (MVP) — 반드시 구현
- 집중 시간 설정 (프리셋 15/25/45분 + +/- 커스텀 조절) 및 타이머 시작/일시정지/중단
- 집중 대기 화면: 실시간 카운트다운, 진행률 표시
- 타이머 종료 시 입질 이벤트 → 챔질(정해진 시간 안에 탭/클릭해야 성공, 실패 시 재시도 또는 놓침 처리) → 릴링(간단한 진행률 채우기 인터랙션, 예: 연속 클릭 또는 홀드)
- 결과 화면: 잡은 물고기(랜덤 또는 집중 시간에 따른 어종 선택 로직), 크기, 등급을 보여주고 도감에 자동 저장
- 로컬스토리지에 세션 기록, 도감, 통계 누적 저장

### 2단계 — 이어서 구현
- 도감 화면: 전체 어종 목록 대비 수집 현황, 등급별 필터
- 통계 화면: 총 집중시간, 총 조과, 연속 집중일수(스트릭), 요일별 집중시간 막대 그래프, 최근 조황 리스트
- 화면 전환에 부드러운 트랜지션 애니메이션

### 3단계 — 여유 있으면
- 다른 탭/창으로 이탈 시 "찌를 놓침" 처리 (Page Visibility API 활용, 짧은 유예시간 허용)
- 사운드: 물소리 배경음, 입질 알림음, 릴 감는 소리
- 미끼/찌 스킨 등 코스메틱 (집중으로 모은 재화로 구매)
- PWA 설치 지원 + 백그라운드 알림

## 데이터 모델 제안

localStorage에 아래와 비슷한 구조로 저장하는 걸 제안해:

```json
{
  "settings": { "lastDuration": 25, "selectedBait": "떡밥" },
  "stats": {
    "totalFocusMinutes": 860,
    "totalCatches": 12,
    "currentStreakDays": 6,
    "weeklyFocusMinutes": [40, 68, 48, 96, 56, 80, 22]
  },
  "collection": [
    { "id": "goldfish", "name": "황금붕어", "rarity": "rare", "caught": true, "sizeCm": 32, "caughtAt": "2026-09-01T15:14:00" }
  ],
  "sessionLog": [
    { "date": "2026-09-01", "durationMin": 25, "fishId": "goldfish", "biteReactionSec": 0.4 }
  ]
}
```

어종 목록(도감에 나온 6종 이상: 붕어/황금붕어/피라미/메기/잉어 등)과 각각의 등급(일반/희귀/전설), 사이즈 범위는 자유롭게 정의해도 되지만 목업 도감 화면에 나온 이름들과는 일치시켜줘.

## 개발 순서 제안

1. 화면 골격 + 라우팅(화면 전환) 구조부터 잡고, 시작 → 집중 화면까지 실제 타이머 동작 구현
2. 입질 → 챔질 → 릴링 → 결과 이벤트 체인 구현
3. 도감/통계 화면과 localStorage 연동
4. 디테일: 트랜지션, 이탈 감지, 사운드
5. 마지막에 반응형/모바일 점검

## 주의사항

- 목업 파일들의 인라인 스타일 값(색상 hex, 여백, 폰트 크기 등)을 최대한 그대로 가져와서 실제 CSS로 정리해줘. 임의로 디자인을 바꾸지 말 것.
- 목업에 있는 숫자/텍스트(예: "25", "황금붕어 32cm", "6일 연속")는 전부 예시 데이터니까 실제로는 동적으로 계산해서 채워줘.
- 지금은 기능 구현이 목표니까 과한 시각 효과보다는 목업에 있는 수준의 톤을 유지하면서 동작을 완성하는 데 집중해줘.
